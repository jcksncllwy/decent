node_modules/
dist/
build/
.svelte-kit/
*.log
.DS_Store

# Decent runtime data (per-user pzp node state — never commit)
.decent/
*.decent-data/

# Hub runtime state (its keypair + member list, created on first run)
packages/hub/vendor/data/

# Env
.env
.env.local
